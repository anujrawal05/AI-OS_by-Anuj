{
  "name": "all-in-one-ai-solution",
  "version": "1.0.0",
  "description": "AI-OS Powered by A.R. Labs",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}